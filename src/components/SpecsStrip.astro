<section class="specs specs-strip" data-specs>
  <div class="specs-inner">
    <div class="spec-item anim anim-up anim-d1" data-spec-item>
      <span class="spec-label">Device</span>
      <span class="spec-value">Redmi Note 7 Pro</span>
    </div>
    <div class="spec-divider"></div>
    <div class="spec-item anim anim-up anim-d2" data-spec-item>
      <span class="spec-label">Codename</span>
      <span class="spec-value">violet</span>
    </div>
    <div class="spec-divider"></div>
    <div class="spec-item anim anim-up anim-d3" data-spec-item>
      <span class="spec-label">SoC</span>
      <span class="spec-value">Snapdragon 675</span>
    </div>
    <div class="spec-divider"></div>
    <div class="spec-item anim anim-up anim-d4" data-spec-item>
      <span class="spec-label">Status</span>
      <span class="spec-value spec-value--active">Active</span>
    </div>
  </div>
</section>

<style>
  .specs {
    padding: 3rem 2.5rem;
    position: relative;
    z-index: 1;
  }

  .specs-inner {
    max-width: 1200px;
    margin: 0 auto;
    background: var(--bg-surface);
    border: 1px solid var(--border);
    padding: 1.5rem 2rem;
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .spec-item {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }

  .spec-label {
    font-family: var(--font-mono);
    font-size: 0.6rem;
    font-weight: 600;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: var(--text-secondary);
  }

  .spec-value {
    font-family: var(--font-mono);
    font-size: 0.85rem;
    font-weight: 600;
    color: var(--text-primary);
  }

  .spec-value--active {
    color: var(--accent);
  }

  .spec-divider {
    width: 1px;
    height: 2.5rem;
    background: var(--border);
  }

  @media (max-width: 768px) {
    .specs {
      padding: 2rem 1.25rem;
    }

    .specs-inner {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 1.25rem;
      padding: 1.25rem;
    }

    .spec-divider {
      display: none;
    }
  }
</style>
